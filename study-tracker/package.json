{
  "name": "ml-platform-study-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node --check app.js && node --check core.js && node --check data.js && node --check sw.js",
    "serve": "python3 -m http.server 4173"
  }
}
